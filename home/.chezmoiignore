{{/* Patterns are matched against TARGET paths (relative to $HOME). */}}

{{/* kitty is a desktop GUI terminal: apply on macOS/Linux desktops, but skip
     inside containers / Codespaces / headless (Docker + Podman markers). */}}
{{ if or (stat "/.dockerenv") (stat "/run/.containerenv") (env "REMOTE_CONTAINERS") (env "CODESPACES") }}
.config/kitty
{{ end }}
